# LunaCrypto Web App Placeholder

The product backend boundary now reserves `apps/web/` for the future frontend.
The current implementation keeps the frontend out of scope and exposes product
data through the NestJS API in `apps/api/`.
